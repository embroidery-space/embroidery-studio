# Planning

When you write implementation plans, do not specify exact commands for verifying code.
Instead, refer to a dedicated `embroiderly-verify` skill, which conatins all the instructions about running various checks.

You can specify only manual checks under the "Verification" section.
These manual check should be executed using the Chrome DevTools MCP.
